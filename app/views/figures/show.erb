<table>
  <tr>
    <th>Name</th>
  </tr>
  <tr>
    <td><%= @figure.name %></td>
    <% @figure.titles.each do |title|  %>
    <td><%= title.name %></td>
    <% end %>
  </tr>
<table>
<a href="/figures/<%= @figure.id %>/edit">edit</a>
